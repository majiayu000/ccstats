{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to use Tauri core APIs and ccstats commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
